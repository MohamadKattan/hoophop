// this class for save latitude + longitude driver who is close to rider in 5 km
class NearByAvilbleDriver {
  String key;
  double latitude;
  double longitude;

  NearByAvilbleDriver({this.key, this.longitude, this.latitude});
}
